services:
  - type: web
    name: monthsary
    env: node
    buildCommand: npm install
    startCommand: npm start
    headers:
      - source: /
        headers:
          - key: Cache-Control
            value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
